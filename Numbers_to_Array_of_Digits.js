// Write a function that takes a number
// and returns an array of its digits.


function numToDigitsArr(x){
	var digitsArray =[];

	for (i=0; i<=x;i++){
		var newitem=digitsArray.push(i);
		}
		console.log("New Array is " + digitsArray)
	}


numToDigitsArr(10);